- Indexed bindings against a EF provider
	- Primitive collections translate on SQLite, PostgreSQL and SQL Server; a Dictionary maps on none of them
	- A navigation collection can effectively be a subquery with no ORDER BY, so [0] is arbitrary, if the collection does not have implicit ordering
	- Worth revisiting if EF gains an ordered form, or refusing the navigation case up front
