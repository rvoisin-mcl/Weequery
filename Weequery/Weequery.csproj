<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<Description>Dynamic filtering, sorting and paging for IQueryable, driven by data rather than code. A condition built in code, parsed from a compact query language, or deserialized from a client becomes an expression tree and is applied to a query. You declare which properties are filterable, and callers can filter on those and nothing else. Works with EF Core, where conditions become SQL with values passed as parameters, and in memory over the same data. No dependencies.</Description>
		<PackageTags>iqueryable;linq;efcore;entity-framework-core;filtering;sorting;paging;expression-trees;dynamic-query;query-language</PackageTags>
		<PackageReleaseNotes>
Weequery 3.1.0.

BEHAVIOUR. BindingResolutionSettings.Default now leaves out anything marked [NotMapped], so BindResolve and
ResolveBindables with no settings no longer bind a property the model says is not a column. A caller filtering
in memory on one loses it; assign IgnoreAttributes = [] to keep the 3.0 behaviour.

Added: BindingResolutionSettings.IgnoreAttributes. A property carrying a listed attribute is not bound, nor is
anything below it, nor a property whose type carries one, nor a quantifier over a collection whose element type
does. Assigning a set replaces the default one, so spread Default.IgnoreAttributes in to add to it. An entry
that is not an attribute type is refused.

Faster: BindResolve keeps what it binds, collections included, for the process, under the depth, the settings
and the use it was asked for. A repeated call walks nothing and builds nothing, and costs about what binding a
cached request set does. Settings are compared by what they hold, so settings built fresh for every request still
find the entry.

The companion packages are released alongside at the same version.
		</PackageReleaseNotes>
	</PropertyGroup>

	<ItemGroup>
		<!-- The test project reaches for helpers that are not part of the published surface -->
		<InternalsVisibleTo Include="Tests" />
	</ItemGroup>

	<ItemGroup>
		<!-- Packed so it becomes the description on nuget.org, see PackageReadmeFile -->
		<None Include="..\README.md" Pack="true" PackagePath="\" />
	</ItemGroup>

</Project>
